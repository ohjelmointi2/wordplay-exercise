package wordplay;

import java.util.ArrayList;
import java.util.List;

public class NamesReader {

    /**
     * This method reads the given CSV file and returns a list containing the names
     * listed in the file. CSV headers and name counts are omitted.
     *
     * @param file eg. "etunimitilasto-naiset-ensimmainen.csv"
     * @return list of names, such as ["Anne", "Tuula", "Päivi", "Anna"]
     */
    public List<String> readNames(String file) {
        // TODO: Read names from the given file. Strip unnecessary headers
        // from the beginning of the file and remove counts from each row.
        return new ArrayList<>();
    }
}
